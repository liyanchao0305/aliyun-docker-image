    - name: Simple Mirror Test
      run: |
        set -e
        echo "🔍 检查 images.txt 文件..."
        if [ ! -f "images.txt" ]; then
          echo "❌ images.txt 文件不存在"
          exit 1
        fi
        
        echo "📄 images.txt 内容:"
        cat images.txt
        echo ""
        
        # 只处理第一行进行测试
        first_line=$(head -n 1 images.txt | sed '/^#/d' | sed '/^$/d')
        if [ -z "$first_line" ]; then
          echo "❌ 没有找到有效的镜像行"
          exit 1
        fi
        
        echo "🚀 处理第一个镜像: $first_line"
        
        # 提取镜像名称
        image=$(echo "$first_line" | awk '{print $NF}')
        echo "镜像名称: $image"
        
        # 拉取镜像
        docker pull $image
        
        # 推送到阿里云
        image_name_tag=$(echo "$image" | awk -F'/' '{print $NF}')
        new_image="registry.cn-beijing.aliyuncs.com/liyanchao/$image_name_tag"
        
        echo "🏷️  标记镜像: $image -> $new_image"
        docker tag $image $new_image
        
        echo "📤 推送镜像: $new_image"
        docker push $new_image
        
        echo "✅ 测试完成: $new_image"