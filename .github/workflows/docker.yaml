    - name: Public Registry Test
      run: |
        echo "=== 测试公共仓库 ==="
        # 测试推送到 Docker Hub（对比验证）
        docker pull hello-world
        docker tag hello-world hello-world-test-$(date +%s)
        echo "Docker 基础功能正常"
        
        # 测试阿里云认证
        echo "认证信息配置完成，请检查阿里云控制台权限设置"